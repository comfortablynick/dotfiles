---
# defaults file for pkg
pkg_home: "{{ ansible_facts.user_dir }}"
pkg_aur_helper_srcdir: "{{ pkg_home }}/src"

pkg_apt_packages: []

pkg_pacman_packages: []

pkg_aur_packages: []

# system python3 packages
pkg_python_packages: []

pkg_pip3_path: /usr/bin/pip3

pkg_pacman_mirrorlist_age: "1w"
