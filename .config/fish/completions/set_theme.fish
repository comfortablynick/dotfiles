# Completion for set_theme
# Set FISH_THEME / FISH_SSH_THEME env vars

complete -c set_theme -f -s h -l help       -d 'show help text and exit'
complete -c set_theme -f -s s -l ssh        -d 'set theme for SSH connections'
