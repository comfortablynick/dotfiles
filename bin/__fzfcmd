#!/usr/bin/env bash

# Check if we should use fzf or fzf-tmux
# Fallback in case certain fzf scripts aren't installed as shell fns
[ -n "$TMUX_PANE" ] && { [ "${FZF_TMUX:-0}" != 0 ] || [ -n "$FZF_TMUX_OPTS" ]; } &&
    echo "fzf-tmux ${FZF_TMUX_OPTS:--d${FZF_TMUX_HEIGHT:-40%}} -- " || echo "fzf"
