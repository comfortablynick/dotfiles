#!/usr/bin/env bash
# Update/build Neovim

src_dir="$HOME/src/neovim"

if git_repo_has_upstream_changes "$src_dir"; then
    command git -C "$src_dir" reset --hard origin/master \
    && _build_neovim "$src_dir"
else
    echo "Neovim has no upstream changes." >&2
fi
